apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dotnet-api.fullname" . }}-test-connection"
  labels:
{{ include "dotnet-api.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "dotnet-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
