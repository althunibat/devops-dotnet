apiVersion: v1
kind: Service
metadata:
  name: {{ include "dotnet-api.fullname" . }}
  labels:
{{ include "dotnet-api.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "dotnet-api.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
